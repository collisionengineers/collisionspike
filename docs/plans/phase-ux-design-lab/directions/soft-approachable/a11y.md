# Accessibility audit — "Warm Hearth" (soft-approachable direction)

**Scope:** static WCAG-2.1-AA audit of the runnable mockups in this folder —
`index.html` (Inbox cockpit), `queues.html` (Queues), `case-detail.html` (Case CCPY26050).
**Method:** source inspection of rendered markup + computed contrast ratios (WCAG relative-luminance).
**Auditor role:** accessibility-engineer. I audit and gate; fixes are routed to the owning agent.

> Note on palette: this direction uses its own *warm-hearth* palette (teal `#1B7163` primary, amber
> `#E8A33D` accent) — it is **not** the frozen CE/Fluent brand. I audit it against the generic WCAG-AA
> floor plus the brand-agnostic principle the CE rules encode (**"amber never white-on-amber"**,
> shape-coded *and* colour-coded status). The frozen CE rules in `docs/design/THEME-MAPPING.md` apply at
> the production-port stage, not here.

---

## Verdict: **GATED — do not converge until the 4 blockers below are fixed.**
## Score: **3 / 5**

Strong, deliberate a11y foundation (focus, reduced-motion, landmarks, shape-coding, ~95% of contrast).
But there are **four real WCAG blockers** — one of them Level **A** (keyboard) — that disqualify it from
winning convergence in its current state. All four are mechanical, low-risk fixes.

---

## What's already right (keep these)

- **Visible focus indicator** — a robust `:focus-visible` halo (`box-shadow: 0 0 0 2px surface, 0 0 0 5px
  #1B7163`, a 3px teal ring) applied to `a, button, input, select, textarea, summary, [tabindex]`.
  ~5.2:1 against the warm surfaces. This is the standout strength.
- **Reduced motion** — all three files ship `@media (prefers-reduced-motion: reduce){ * { animation:none;
  transition:none; scroll-behavior:auto } }`. The Held "breathe" bead animation is correctly killed.
- **Landmarks & headings** — `<aside>/<main>/<header>/<footer>`, both `<nav>`s `aria-label`led, one `<h1>`
  per page, clean `h2`→`h3` nesting.
- **Status is not colour-alone** — every status carries glyph **+** colour **+** text (`▲ Review`,
  `◆ Held`, `✔ Ready`, `◦ New`). Due-ramp pips are backed by text (`3d over`) and `aria-label`. Meets the
  "shape-coded AND colour-coded" rule.
- **Most contrast passes well** — body text 8:1, muted text 5.0–6.0:1, and all *dark-on-tint* status pills
  (`accentText#8A5A10` on `accentTint#FBEBCF` = **5.04:1**, ready 5.32, review 5.61) clear AA. The Held
  pills that use dark-on-amber are the *correct* pattern.
- **Good ARIA spots** — pipeline beads each `aria-label`led (`"Review: 3 cases — needs you"`); provenance
  glyphs carry `aria-label` (`reviewed`/`conflict`/`needs review`); search input labelled via wrapped
  `sr-only` span; the Evidence exclude-photo `.sw` is a fully operable `role="switch"` (aria-checked +
  Space/Enter + label); case-detail tabs use correct `role="tab"/"tabpanel"` + arrow-key roving.

---

## Blockers (must fix before this direction can win) — Severity: CRITICAL

### B1 · Form controls have no programmatic label — `case-detail.html`  ·  WCAG 1.3.1 / 3.3.2 / 4.1.2 (A/AA)
The entire Fields panel pairs a `<label>` with an `<input>` as **siblings** — the label has no `for` and the
input has no `id`/`aria-label`:
```html
<label class="flex ..."><span>1 · Work provider</span> ...prov badge...</label>
<input class="fld" value="ABC Claims (CCPY)" />   <!-- not associated -->
```
Screen-reader users hear "edit text" with no name. Affects **all 12 numbered fields**, the Mileage-unit and
VAT `<select>`s, the **Address IBA reason** input, the **Chaser** textarea, and the **Notes** textarea
(placeholder-only). ~16 controls. (The header search input is done correctly — copy that pattern: wrap the
input, or add `id`+`for`.)
→ **Owner: stitch-prototyper.**

### B2 · Queue grid rows are mouse-only — `queues.html`  ·  WCAG 2.1.1 Keyboard (Level **A**)
Desktop/tablet rows open a case via `<tr ... onclick="location.href='case-detail.html'">` with no focusable
element, no `tabindex`, no link. Keyboard and screen-reader users **cannot open a case** from the grid. (The
phone `.qcard` view correctly uses `<a>` — only the `<table>` is broken.)
→ Make each row reachable/operable (anchor in the first cell, or `tabindex=0` + role + Enter/Space handler).
→ **Owner: stitch-prototyper.**

### B3 · Collapsed sidebar nav links have no accessible name — all three pages  ·  WCAG 4.1.2 (A)
The rail is `hidden md:flex w-[68px] lg:w-60`; the link text/count spans are `hidden lg:block`. Between the
`md` and `lg` breakpoints the rail is icon-only and each link resolves to **`<a><svg></svg></a>`** — empty
accessible name (SVGs have no `<title>`, no `aria-label`). Affects Cockpit/Inbox/Queues/Cases/Manual
intake/Admin·Corpus/Settings. (The mobile bottom-nav keeps visible text labels and is fine.)
→ Add `aria-label` to each rail `<a>` (always-on, independent of the text span).
→ **Owner: stitch-prototyper.**

### B4 · White-on-fill badges fail contrast — `index.html`, `queues.html`, `case-detail.html`  ·  WCAG 1.4.3
| Use | Pair | Ratio | Need | Result |
|---|---|---|---|---|
| **`◆ Held` solid badge** `bg-accent text-white` (index R2 "Live work" tile; bead corner dot) | white on `#E8A33D` | **2.16:1** | 4.5 | **FAIL** — violates "amber never white-on-amber" |
| **`▲ Review` solid badge** `bg-review-500 text-white` (index R2 tile) + mobile-nav count "3" | white on `#D2685E` | **3.57:1** | 4.5 | **FAIL** (passes 3:1 graphic, not text) |
| Toast tick / readiness `▲` icon discs `bg-review-500`/`bg-ready-500 text-white` | white on `#D2685E`/`#5B9A6E` | 3.57 / 3.34 | 3.0 (decorative, aria-hidden) | borderline-OK as graphics |

Fix the two **text** badges: use the dark-on-tint treatment already used elsewhere — `bg-accent-tint
text-accent-text` (5.04:1) for Held, `bg-review-100 text-review-700` (5.61:1) for Review — instead of
white-on-fill. The amber Held tile is the headline: it is exactly the white-on-amber failure the CE brand
rule forbids.
→ **Owner: ui-visual-designer.**

---

## Moderate

- **M1 · Address suggestion radios unlabeled** (`case-detail.html`) — the 3 ranked `<input type="radio">`
  sit next to a `<div>` with no `<label>`/`for` wrap (only the IBA radio is wrapped). Unnamed control
  (4.1.2) **and** a 16px hit target. → stitch-prototyper (label) + mobile-ux-designer (target).
- **M2 · `role="tablist"` misused** (`queues.html`) — the partition bar is `role="tablist"` but its children
  are plain `<button>`s (no `role="tab"`, no `aria-controls`/`tabpanel`). AT announces a tablist with no
  tabs. It's a filter group → use `role="group"`/toolbar, or make them real tabs. → stitch-prototyper.
- **M3 · Selected state not exposed to AT** — `.seg-chip.is-on` (index), `.part.is-on` / `.facet.is-on` /
  density toggle (queues) signal selection by **colour/style only**; no `aria-pressed`/`aria-current`/
  `aria-selected`. SR users can't tell which filter is active. → stitch-prototyper.
- **M4 · Touch targets < 44px (pervasive)** — refresh btn `w-8 h-8`=32px; triage Confirm/Reclassify &
  "→ Case" & case actions `py-2`≈36px; seg-chips/partitions/facets/tabs ≈30–36px; density toggle ≈28px;
  `.sw` switch 38×22px; radios 16px. None meet 44×44 (WCAG 2.5.5 AAA, but the CE floor is 44px).
  → mobile-ux-designer.

## Minor

- Decorative geometric glyphs (`▲ ◆ ✔ ◦ ◌`) inside badges are **not** `aria-hidden`, so SRs verbalize
  "up-pointing triangle Review" etc. Add `aria-hidden="true"` to the glyph spans (the text label stays).
- `index.html` pipeline `<ol>` contains `<div>` thread-lines and `<a>`-wrapped beads as direct children —
  non-`<li>` children of a list is invalid; wrap beads in `<li>` (mostly done) and move the decorative
  thread `<div>`s out of the list flow.
- Queues channel column uses `title="Email"` on a `<td>` holding `✉` — `title` is unreliable for AT; add an
  `sr-only` text or `aria-label`.
- `index.html` "Engineer (reserved)" item is `<a href="#" aria-disabled="true">` — still focusable and
  navigable; give it `tabindex="-1"` / remove `href`, or render as a disabled `<button>`.
- Disabled "Submit to EVA" / "Open in Box" use `bg-primary/40 text-white/90` (~low contrast) — acceptable,
  WCAG exempts disabled controls, and they carry `disabled` + `title`.

---

## Routing summary
| Owner | Items |
|---|---|
| **stitch-prototyper** (markup/ARIA) | B1, B2, B3, M1(label), M2, M3, minor glyph/list/title fixes |
| **ui-visual-designer** (colour) | B4 (Held + Review badge contrast) |
| **mobile-ux-designer** (targets) | M4, M1(target) |

**Gating decision for convergence: NO-GO** until B1–B4 are resolved. Re-audit after fixes; the foundation
(focus, motion, landmarks, shape-coding) is strong enough that clearing the four blockers should lift this
to ~4.5/5.
