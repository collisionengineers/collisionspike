# Accessibility audit — `agenda-ops` (Day Ledger) · directions-r2

**Scope:** static WCAG 2.1/2.2-AA audit of `index.html` (Cockpit), `queues.html`, `case-detail.html`.
**Method:** source + rendered-token analysis; contrast ratios computed from the CSS custom properties.
**Date:** 2026-06-25 · **Auditor:** accessibility-engineer

**Verdict: CONDITIONAL — 3 / 5.** Strong landmark/labelling bones (named `nav`/`main`/`aside`,
`aria-label` on inputs and icon-buttons, an `sr-only` data table behind the chart, `aria-live` on the
result count, status never encoded by colour alone). But several **real AA gaps** repeat across all three
pages: a sub-3:1 focus ring, pervasive `--ink4` text below 4.5:1, white-on-light meter labels, missing
toggle/tab state (`aria-selected`/`aria-pressed`), icon-only channel names, no heading semantics, and a
drag-only reorder with no keyboard path. None are catastrophic; all are fixable. **Gate: do not let this
direction converge until the focus-ring contrast, `--ink4` text, and tab/toggle ARIA are cleared.**

---

## Per-page scorecard

| Dimension | index (Cockpit) | queues | case-detail |
|---|---|---|---|
| Colour contrast | ⚠ fails (ink4, meter segs) | ⚠ fails (ink4) | ⚠ fails (ink4) |
| Visible focus | ⚠ present but ~2.5:1 | ⚠ present but ~2.5:1 | ⚠ present but ~2.5:1 |
| Semantic structure / landmarks | ⚠ no h1/headings | ✖ no `<main>` + no h1 | ⚠ no h1/headings |
| ARIA / labels on controls & icons | ⚠ ok-ish | ✖ icon-only channel, no aria-sort/pressed | ✖ tabs no aria-selected, switches no aria-pressed |
| Keyboard operability (rows/tabs) | ✔ roving j/k/↵ works | ✔ rows work; sort headers dead | ✖ drag reorder keyboard-inaccessible |
| Touch targets | ⚠ row action buttons ~20px | ⚠ facet/pick ~24px | ✖ exclude switch 30×17px |
| Reduced motion | ✖ no `prefers-reduced-motion` | ✖ none | ✖ none (+ smooth-scroll ungated) |

✔ pass · ⚠ partial / borderline · ✖ located failure

---

## Located failures by category

### 1. Colour contrast (WCAG 1.4.3 text 4.5:1 / 1.4.11 non-text 3:1)

Computed ratios (token → background):

- **`--ink4 #9AA093` fails everywhere it carries text — 2.6:1 / 2.4:1 / 2.3:1** on surface / paper / sunk.
  This is the single most pervasive fail. Located uses: timestamps `.when` and `.sub` labels
  (`index.html:225,227`), `.cl-item .tag` (`243`), aging axis/`.lab` (`134`), burndown `.bl` (`237`);
  `.qsearch .kb` and `.prov .code` (`queues.html:98,138`); `.frow .idx`, `.docitem .meta`, `.stamp.none`,
  `.olrow .ol-tag`, `.fact .lock` (`case-detail.html:142,183,162,208,292`). Needs ~`#7C8278` or darker.
- **Meter segment labels — white text on light bars fail:** `white on s-new #8A9183 = 3.25`,
  `s-parse #7E97AB = 3.04`, `s-ready #5E8C57 = 3.91` (need 4.5:1 for the ~10px bold labels)
  (`index.html:90–95`, markup `341–347`). `s-held 4.50`, `s-subm 6.92`, `s-box 9.11` pass.
- **`--ink3 #6B7268` is marginal on darker grounds:** 4.45:1 on `--paper`, 4.24:1 on `--sunk` — both
  under 4.5. Hits the cleared-log and imported-details panels (`.clearedlog`/`.imported` use `--sunk`):
  `.fact .k`, `.sb-h`, `.cl-num .l` (`case-detail.html:290,267`; `index.html:234`). On `--surface` it is
  4.74 (passes). Nudge `--ink3` ~one step darker to clear it on every ground.
- **Amber as a meaning-bearing glyph fails:** `--ds-bar #C98A1E = 2.81:1` on surface — used for the
  `.outst .vg ⚑` flag and `.divtab.ready .star ★` (`queues.html:153,80`) and dayplan caption. Below 3:1
  non-text and far below 4.5:1 as a glyph. (This is the brand "amber never low-contrast" rule in spirit.)

### 2. Visible focus (WCAG 2.4.7 / 2.4.11 / 1.4.11)

- A single global indicator exists — `:focus-visible{outline:2px solid var(--pine-ring)}`
  (`index.html:37`, `queues.html:35`, `case-detail.html:35`) — good that it's centralised, **but
  `--pine-ring #5FA993` resolves to only 2.65:1 vs `--surface`, 2.48:1 vs `--paper`, 2.35:1 vs the
  `--pine-tint` selected-row** — under the 3:1 an indicator needs. On the pine-tinted current row/cell the
  ring nearly vanishes. Darken the focus colour (e.g. `--pine` `#1C6552`, 6.9:1) or add a contrasting
  inner/outer halo so it reads ≥3:1 on every background.

### 3. Semantic structure / landmarks (WCAG 1.3.1 / 2.4.6)

- **No `<h1>` and no heading elements** on any page. Every visual heading is a styled `div`/`span`
  (`index.html:317` day/date, `queues.html:199` "Queues", `case-detail.html:359` id row). The only real
  heading in the set is `.kbref h3`. Screen-reader heading navigation is impossible. Promote the page
  title and each section header (`.sr-h`, `.subhead`, `.cl-head .t`, `.band .lab`) to `h1`–`h3`.
- **`queues.html` has no `<main>` landmark** — content sits in `<section class="ledger">` with no main
  region (`queues.html:216`). Wrap the ledger in `<main>`.
- Otherwise landmarks are good: `nav aria-label`, `aside aria-label`, `main` on index/case-detail, the
  chart `role="img"` + `sr-only` `<table>` fallback (`index.html:381,391`), pipeline-spine `role="img"`
  with a described label (`case-detail.html:395`).

### 4. ARIA / labels on controls & icons (WCAG 4.1.2)

- **Tabs missing required state:** `role="tab"` buttons carry no `aria-selected`, no `aria-controls`, and
  panels no `aria-labelledby` (`case-detail.html:408–416`; JS only toggles a class, `819–825`).
  `role="tab"` without `aria-selected` is an ARIA contract violation. Add `aria-selected`, link
  `aria-controls`/`id`, and roving `tabindex`.
- **Toggle state never exposed:**
  - Evidence "Exclude (reflection)" controls are `<button class="switch">` with `aria-label` but **no
    `aria-pressed` / no `role="switch"`+`aria-checked`** (`case-detail.html:574,581,588,595`); state is
    colour/position only.
  - Review reason `.facet` buttons toggle on/off with no `aria-pressed` (`queues.html:229–232`).
  - Chaser channel `.seg-ctrl` Email/WhatsApp has no `aria-pressed` (`case-detail.html:677–680`).
- **Icon-only controls with no accessible name:** channel cells `<span class="chn" title="Email|WhatsApp">✉/⌖`
  and the `.dupflag ⤬` rely on `title` on a non-focusable `span` (`queues.html:254,259,270,281,…`). `title`
  on a `span` is not a reliable accessible name; channel is conveyed by glyph alone. Add `aria-label` /
  `sr-only` text (e.g. "Channel: WhatsApp").
- **Sort headers are inert and misleading:** `<th class="srt">` show a pointer cursor and a `▲` but are
  not buttons, have no `aria-sort`, and the JS implements no sort (`queues.html:242–247`). Either make them
  `<button>` headers with `aria-sort`, or drop the affordance.
- **Overflow menu:** `aria-haspopup="true"` but no `aria-expanded` toggling and no keyboard menu semantics
  (`case-detail.html:382`, JS `838–840`).
- **Required field not programmatic:** VAT status shows a visual `required` + error text but no
  `aria-required`, and the error (`.errmsg`) is not tied via `aria-describedby` (only `aria-invalid` is
  set) (`case-detail.html:483–491`). Same for the IBA-reason "required" (`644–648`).

### 5. Keyboard operability (WCAG 2.1.1 / 2.5.7)

- **Good:** agenda roving cursor `j/k/↵` with `tabindex="0"` rows and a focus handler
  (`index.html:400,753–785`); queue rows Enter-to-open (`queues.html:252,357`); `/` focuses search; `?`
  opens the key reference; tabs are real buttons (Enter/click).
- **Drag-to-reorder has no keyboard alternative** — the EVA photo-order list is `draggable` divs with only
  pointer DnD handlers (`case-detail.html:601–607`, JS `848–854`). Fails 2.1.1 and the WCAG 2.2 dragging
  criterion (2.5.7). Add up/down buttons or `↑/↓`-key move on focused rows.
- **Dialogs don't manage focus:** submit modal and the `kbref` overlay set `role="dialog" aria-modal` but
  focus isn't moved in on open, isn't trapped, the background isn't `inert`, and focus isn't restored on
  close (`case-detail.html:776,857–864`; `index.html:731,769–773`). Esc-close is also suppressed while a
  field is focused (`case-detail.html:872–877`).

### 6. Touch targets (WCAG 2.5.8 AA = 24×24; 2.5.5 AAA = 44×44)

- **Below 24px:** the evidence exclude `.switch` is **30×17px** (`case-detail.html:195`) — under the AA
  minimum. Agenda row-action buttons `.rowacts .ra` compute to ~20px tall (`padding:3px 7px;font:11px`,
  `index.html:171`). Evidence `.thumb select` ~22px (`193`). The modal `.x` close glyph has no padding.
- **Borderline (~24px):** `.facet` (`queues.html:105`), `.sugg .pick` (`case-detail.html:222`).
- **Pass:** `.navitem` 48×46, `.iconbtn`/`.ovbtn` 26–32, `.tray-row` `min-height:44`, `.divtab` full-row.

### 7. Reduced motion (WCAG 2.3.3 AAA / brand floor)

- **No `@media (prefers-reduced-motion: reduce)` anywhere** in the three files. Motion is light
  (`.switch`/chevron `transition .15s` — `case-detail.html:168,195`), so severity is low, but the
  programmatic `scrollIntoView({behavior:'smooth'})` on the readiness bar (`case-detail.html:868`) and the
  roving-cursor `scrollIntoView` should respect the reduced-motion preference. Add the media query and gate
  smooth scrolling.

---

## Positives (keep)

- Centralised `:focus-visible` (just recolour it), `.sr-only` utility, and an `sr-only` `<table>` fallback
  for the aging chart — exemplary.
- Status is **never colour-alone**: every row/badge pairs a glyph **and** a text label; bands and queue
  partitions are text-labelled.
- `aria-label` on every text input and on icon-only buttons that matter (Refresh), `aria-current` on nav,
  `aria-live="polite"` on the queue result count, `aria-disabled`+`title` explaining *why* Submit is locked.

---

## Top fixes (priority order)

1. **Recolour the focus ring** to ≥3:1 on surface/paper/pine-tint (use `--pine #1C6552` or add a halo). —
   owner: ui-visual-designer.
2. **Fix `--ink4` (and marginal `--ink3` on sunk/paper)** so all body/meta text clears 4.5:1; recolour the
   white-on-light meter segments (`s-new/s-parse/s-ready`). — owner: ui-visual-designer.
3. **Add tab + toggle ARIA:** `aria-selected` on `role="tab"`; `aria-pressed` (or `role="switch"`+
   `aria-checked`) on exclude switches, review facets, and the channel segmented control. — owner:
   stitch-prototyper.
4. **Give icon-only controls accessible names** (channel ✉/⌖, dup ⤬) and either wire up `aria-sort`
   buttons or drop the sort affordance. — owner: stitch-prototyper.
5. **Add a keyboard path for the photo-order reorder** (↑/↓ or move buttons). — owner: stitch-prototyper.
6. **Introduce a real heading hierarchy** (`h1` per page + section headings) and add `<main>` to
   `queues.html`. — owner: stitch-prototyper.
7. **Bump sub-24px targets** — exclude switch, agenda row-action buttons, thumb role select. — owner:
   mobile-ux-designer.
8. **Add `prefers-reduced-motion`** and gate the smooth-scroll calls. — owner: stitch-prototyper.
