# Changes — TKT-281: Mount the guided-capture staff panel into CaseDetail

## Status
backlog

## Commits
No code changes.

## Files touched
- n/a

## Summary
Renumbered from collisioncapture's `CCAP-012` during the TKT-278 repository merge. Deliberately **not**
closed as a duplicate of TKT-200: verification found the component and its API plumbing are fully built
and unit-tested, but never mounted into the live CaseDetail view, so today no staff user can actually
create a guided-capture session from a case. Stays `backlog` until scheduled — a small, precisely-scoped
wiring fix, not new component work.
