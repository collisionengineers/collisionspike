# PLAN-015 alpha cutover — execution record, 2026-07-21 (~13:30–15:15Z)

Operator-authorized execution of [docs/operations/alpha-testing.md](../../../../operations/alpha-testing.md)
("do the runbook yourself", 2026-07-21). Identity: `digital@collisionengineers.co.uk` (az CLI +
Exchange device-code sign-in completed by the operator). All timestamps UTC. Same-day banking per
the App Insights retention rule.

## Phase 0 — deploy dark (~13:45–14:00Z)

- Source: `main` @ `8bd558dc` (post PR #161/#162), clean tree. Artifacts via `npm run
  package:deploy`; contents verified per the artifact rule (data-api: `@azure/functions` +
  `@img/sharp-linux-x64` + `@img/sharp-libvips-linux-x64`; orchestration: `@azure/functions` +
  `durable-functions`). One transient `npm.cmd` 0xC0000409 crash on first packaging run;
  clean re-run succeeded.
- `func azure functionapp publish <app> --javascript` (the artifact carries no language marker —
  the flag is required; worth folding into deployment.md).
- Post-deploy: orchestration **109** functions (was 106; `eva-shadow-submit-starter`,
  `evaShadowSubmitOrchestrator`, `intake-poll` all listed by name); API **146 raw** endpoints
  (unchanged — LIVE_FACTS reconciles to 144 per TKT-257; an earlier same-session claim that the
  deploy added two API functions was wrong and is corrected here).
- `eva-shadow-submit` queue created on `cespkorchstdev01` (created=True).
- Dark proof: `EVA_API_ENABLED`, `EVA_SHADOW_AUTOSUBMIT_ENABLED`, `INTAKE_POLL_ENABLED` absent on
  both apps (readback); `/api/cases` answers 401 (auth wall).

## Phase 1 — Exchange RBAC (14:04–14:38Z, completed after a first expired device code)

- `Connect-ExchangeOnline` device-code auth (first code G3DEWGA4X expired unused; second code
  G6S2965KV completed by the operator).
- `CollisionSpike-Intake-Prod` recipient filter extended. OLD (banked for rollback):
  `((((EmailAddresses -eq 'SMTP:info@collisionengineers.co.uk') -or (EmailAddresses -eq 'SMTP:engineers@collisionengineers.co.uk'))) -or (EmailAddresses -eq 'SMTP:desk@collisionengineers.co.uk'))`
  NEW: the same expression `-or (PrimarySmtpAddress -eq 'instructions@collisionengineers.co.uk')`.
- `Test-ServicePrincipalAuthorization` for `CollisionSpike Graph Intake`
  (5d37a155-2af8-4878-b96a-6faad5207137): `Application Mail.Read` and `Application Mail.ReadWrite`
  `InScope=True` for **all four** mailboxes (instructions@ via the extended Prod scope; engineers@
  additionally via CS-Intake-EngDigital).

## Phase 2 — intake re-scope + quiesce (14:01–14:10Z)

- 14:01:05Z — `GRAPH_INTAKE_MAILBOXES` set on `cespk-orch-dev` to
  `[{"mailbox":"instructions@collisionengineers.co.uk","minIntakeDate":"2026-07-21T14:01:05Z"}]`
  (read back verbatim).
- 14:02:42Z — `POST /api/graph-renew` (host function key): summary pruned all six legacy
  subscriptions (`info@`, `engineers@`, `desk@` Inbox + their three `sentitems:` entries);
  bootstrap of `instructions@` (+ its SentItems) failed Graph 403 `ExtensionError / Access is
  denied` (request-ids e0746f17…, e145654c…).
- 14:41Z — re-POST ×2 after the Phase 1 scope extension: still 403 (request-ids cae71b07…,
  d19cf127…, c1873856…, 315b2963…) despite `InScope=True` — Exchange RBAC **propagation lag**
  (the test evaluates configuration; live enforcement caches lag). An automatic retry loop
  (POST graph-renew every 5 minutes, up to 1 h) was armed; the durable monitor is the ≤6 h
  backstop either way. Subscription state at the time this record was committed is in
  `LIVE_FACTS.mailIntake.status`.
- Quiesce verified: `intake-messages`, `evidence-backfill`, `outlook-move`, `eva-shadow-submit`,
  `sent-messages` and the poison queues all peeked **0 messages**.

## Phase 3 — backups (14:07–15:05Z; all before any deletion)

- **Database**: `pg_dump --role=csadmin -Fc` over the Entra-admin token path from WSL
  (`pg_dump` 18.4 client against server v16) → `collisionspike-prealpha-2026-07-21.dump`
  (7,972,549 bytes; `pg_restore --list` shows **79 TABLE DATA** entries = every live table).
- **Continuity capture**: `case-po-values.csv` (820 Case/POs), `case-po-floor.csv` (0 rows —
  floors were dark), pre-wipe counts: 1,025 cases / 1,991 inbound emails / 20,559 evidence rows /
  390 providers / 79 tables.
- **Blob container** (`evidence` on `cespkevidstdev01`): `az storage copy` bulk download —
  12,896 enumerated, 12,792 completed, 104 failed, 8,855,364,668 bytes. All 104 failures were one
  cause: **Windows case-insensitivity collisions** (Graph ids are case-sensitive base64). A
  reconciliation pass (full remote enumeration = **12,930** — the delta above the scan being
  pre-prune in-flight arrivals landing behind azcopy's enumerator, plus two silent case-twin
  overwrites) fetched every collision-set member and post-scan straggler into
  `evidence-casefix/` under indexed names with a manifest. Final ledger: **12,930/12,930 held,
  toFetch=0** (`reconcile-blobs.mjs`). Backup root: `C:\Users\Alex\collisionspike-backups\2026-07-21\`
  (8.5 GB + dump + CSVs + manifests + scripts).

## Phase 4 — wipe + rebuild (~14:25–14:50Z)

- Pre-flight: newest migrations (2026-07-16 capture-rate-limit, 2026-07-17 tkt226 subtype
  100000016) verified present in baseline; live-vs-baseline table diff showed the only
  live-not-in-baseline tables were **14 historical remediation-backup scratch tables**
  (`backup_2026*`, `tkt081_backup_*`, `tkt129/130_backup_*`, `inspection_address_reseed_backup…`)
  — correctly absent from the definition, preserved in the dump, intended to disappear.
- `DROP SCHEMA public CASCADE; CREATE SCHEMA public; GRANT USAGE ... TO cespk_app;` then
  `database/baseline/*.sql` in filename order (900 last; two benign `DROP TRIGGER IF EXISTS`
  notices), seeds 910/915/916, and 920 via a **resolved-path copy** — the documented
  `-v csvpath=` invocation cannot work because psql performs no variable interpolation inside
  `\copy` (README fix folded into this ticket; `database.md` updated).
- Continuity floors: 27 prefixes from the 820 captured Case/POs, zero unparsed (highlights:
  `QDOS26=811`, `A.QDOS26=228`, `A.PCH26=213`; full set in `floors.sql` in the backup root).
- Probes: **66** base tables (65 baseline + 920's first-run backup table); RLS enabled+forced on
  `case_`, `evidence`, `inbound_email`, `audit_event`, `capture_session`; 390 providers, QDOS
  active; the suggested-address layer reloaded non-empty from the corpus CSV (exact tally in the
  probe output banked in the backup root); 0 cases; 0 emails; 27 floors; `qdos_floor=811`.

## Phase 5 — clear + staff-visible changes (14:55–15:15Z)

- Gate flips on `cespk-api-dev` (read back post-flip): `PUBLIC_CAPTURE_ENABLED=false`,
  `CAPTURE_SESSIONS_ENABLED=false`, `CAPTURE_DIRECT_UPLOAD_ENABLED=false` (closes the TKT-200
  exposure), `DELETE_CASE_IMAGE_ENABLED=false`, `MCP_IMAGE_INGEST_ENABLED=false`,
  `IMAGE_ANALYSIS_ENABLED=false`. `MCP_SERVER_ENABLED=true` retained deliberately.
- Smokes: `POST /api/public/capture/exchange` → **404 {"error":"capture_missing"}**; staff SPA
  root → 200.
- Staff SPA deployed to `cespk-spa-dev` (SWA CLI; `staticwebapp.config.json` copied into `dist`
  — the build does not carry it; CLI run scoped to `apps/web` after a repo-root run
  stack-overflowed in config discovery). Ships TKT-300 + PR #158.
- Blob clear: `az storage blob delete-batch` over the `evidence` container, launched only after
  the backup ledger closed at 12,930/12,930; post-delete probe: **0 blobs remaining** (~15:20Z).
  `cespkorchstdev01` untouched (Durable state).
- Box test folder clear remains the operator's own action (their stated preference).

## Phase 6 — NOT executed

Blocked on the vendor UAT `EVA_CLIENT_ID`/`EVA_CLIENT_SECRET` reaching Key Vault
`cespkevakvufa3ci` (operator/vendor dependency; secret values are never agent-handled). The
hardened `finalize-eva-box` starter is already deployed, so the Phase 6 ordering dependency is
satisfied in advance.

## Residual items at the time of this record

1. Alpha mailbox subscription creation — pending Exchange RBAC propagation (auto-retrying; the
   durable monitor is the backstop). Staff forwards should wait for confirmation.
2. Phase 6 (EVA UAT credentials + three gate flips).
3. Behavioural smokes needing real arrivals (QDOS forward end-to-end, images routing, extract →
   shadow submission, TKT-296's `parseFedApplied`, TKT-034's reg-folder proof).
4. Findings folded back into docs this same day: the `--javascript` publish flag; the 920
   `\copy` interpolation bug; 920's +1 backup table in the post-rebuild count; the case-collision
   hazard in blob backups (recorded here — the runbook's download-batch step should be read with
   this in mind).
