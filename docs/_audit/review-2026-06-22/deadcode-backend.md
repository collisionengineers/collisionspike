# Dead code & legacy fallbacks — backend (Functions, flows, parser) (#7)

This audit reviews the backend (Azure Functions, Power Automate flow definitions, and the vendored
parser) for dead code, orphaned helpers, duplicated logic, and stale repo state that trails the live
deployment. Each finding below was independently re-verified against the repo at HEAD; only confirmed
findings (`verdict.real = true`) are listed, with the verifier's note and any corrections preserved.

**Confirmed findings by severity:** 9 total — 0 critical, 2 high, 2 medium, 3 low, 2 info.

## [HIGH] Parser vendored copy has DIVERGED from the sibling repo (B2 telephone/email extraction), and NO test guards engine-source parity

**Location:** `functions/parser/cedocumentmapper_v2/{rules/engine.py, normalization/normalizers.py, normalization/__init__.py, domain/models.py}` vs `../cedocumentmapper_v2.0/src/cedocumentmapper_v2/...`; `functions/parser/tests/test_schema_vendored_in_sync.py`

**Evidence:** A structural diff of the two trees shows exactly 4 source files differ and the divergence is entirely the ROADMAP-B2 claimant contact extraction, present ONLY in the vendored copy: `domain/models.py` adds `CLAIMANT_TELEPHONE = "claimant_telephone"` / `CLAIMANT_EMAIL`; `normalizers.py` adds `TELEPHONE_RE`, `EMAIL_RE`, `normalize_telephone`, `normalize_email` (~80 lines); `rules/engine.py` adds `_fallback_telephone`/`_fallback_email`/`_CLAIMANT_CONTEXT_WORDS` (~190 lines). The image-based-inspection fix (`IMAGE_BASED_ASSESSMENT = "Image Based Assessment"`) is now present in BOTH copies (that hunk was reconciled), so the live remaining drift is B2 only — the inverse of what memory note `parser-vendored-divergence` describes. Critically, the only sync test, `test_schema_vendored_in_sync.py`, compares the JSON schema (`VENDORED = HERE.parent / "contracts" / "eva-payload.schema.json"`) NOT the engine source, so this divergence is completely unguarded and will keep silently re-opening.

**Recommendation:** Back-port the B2 telephone/email work into `../cedocumentmapper_v2.0` (the sibling is the upstream source of truth per CLAUDE.md) so the two converge, OR explicitly accept the vendored copy as the fork-of-record. Either way add a test that hashes/diffs the vendored engine files (`engine.py`, `normalizers.py`, `models.py`, `normalization/__init__.py`) against the sibling and fails on drift, mirroring how the schema is already guarded.

**Verifier note (confidence: high):** CONFIRMED by re-reading the repo. The vendored parser (`functions/parser/cedocumentmapper_v2/`) has diverged from the sibling (`../cedocumentmapper_v2.0/src/cedocumentmapper_v2/`) and the ONLY drift is the ROADMAP-B2 claimant contact extraction, present vendored-only: `models.py` adds `CLAIMANT_TELEPHONE`/`CLAIMANT_EMAIL` (lines 20-25, 42-43, 60-61); `normalizers.py` adds `TELEPHONE_RE`/`EMAIL_RE`/`normalize_telephone`/`normalize_email` (lines ~100-179); `engine.py` adds `_fallback_telephone`/`_fallback_email`/`_CLAIMANT_CONTEXT_WORDS`/`_CLAIMANT_CONTACT_LABELS` (203 added vs 1 removed lines); `normalization/__init__.py` re-exports them. Verified `diff` of all four files. Critically, the finding's headline claim holds: the image-based fix is now in BOTH copies (`IMAGE_BASED_ASSESSMENT="Image Based Assessment"` at vendored `engine.py:101` AND sibling `engine.py:98`), so the remaining drift is B2 only — the inverse of memory note `parser-vendored-divergence` (whose own text, line 19-22, admits the image-based hunk "was then surgically ported into the vendored copy" and lists reconciliation as an open TODO). The lone parity test, `test_schema_vendored_in_sync.py:16-17`, compares ONLY `contracts/eva-payload.schema.json` (VENDORED vs KEYSTONE), never the engine source; `test_contact_extraction.py` exercises B2 behavior but does not diff against the sibling, and `test_double_encoding.py`'s sibling references are to the instruction fixture corpus, not source parity. So the engine-source divergence is genuinely unguarded and will keep silently re-opening. Not intentional-by-design and not already-handled.

**Verifier correction:** One overstatement: "exactly 4 source files differ" is true only for the Python engine-source files. A full `diff -rq` of the two trees shows 3 additional differences that are intentional-by-design, not drift: `cli.py` and `ui/host.py` exist only in the sibling (dev/UI entry points, correctly excluded from the deployed extraction path), and `providers.json` exists only in the vendored copy (its runtime seed, loaded at `application/service.py:430`). The accurate statement is "exactly 4 Python engine-source files differ in content," not "exactly 4 files differ" across the whole tree. This does not weaken the finding or its high severity.

## [HIGH] Repo intake.definition.json trails live (missing Run_enrich + Run_case_resolve) — a solution re-import would regress live wiring

**Location:** `flows/definitions/intake.definition.json:463` (and the absence of `Run_enrich`/`Run_case_resolve` cards)

**Evidence:** The flow's own top comment warns: 'WARNING — LIVE DIVERGES (verified 2026-06-21): the live CS Intake WAS subsequently wired to call Run_case_resolve (+ Run_enrich) after parse, so auto-merge DOES run live; THIS repo definition trails live — reconcile it (add Run_enrich + Run_case_resolve) before any solution re-import, else a re-import would regress the live wiring.' A grep confirms the repo def contains `Run_classify_persist`/`Run_parse`/`Run_status_evaluate`/`Scope_generate_casepo` but NOT `Run_enrich` or `Run_case_resolve`. This is corroborated by CURRENT_STATUS 'REPO-TRAILS-LIVE' and memory `intake-repo-trails-live`.

**Recommendation:** Reconcile the repo intake def to live now (add the `Run_enrich` and `Run_case_resolve` Run-a-Child-Flow cards with their byte-identical `OnNewEmailV3` trigger) so the committed definition is safe to re-import. Until then, treat the CollisionSpikeFlows solution as not-safe-to-reimport and note it in DEPLOY-RUNBOOK.

**Verifier note (confidence: high):** VERIFIED — finding is accurate. (1) The WARNING comment is real and quoted verbatim at `flows/definitions/intake.definition.json:463` (the top-level workflow comment): "WARNING — LIVE DIVERGES (verified 2026-06-21): the live CS Intake WAS subsequently wired to call Run_case_resolve (+ Run_enrich) after parse, so auto-merge DOES run live; THIS repo definition trails live — reconcile it (add Run_enrich + Run_case_resolve) before any solution re-import, else a re-import would regress the live wiring." (2) Grep confirms the repo def's actions are ONLY `Run_classify_persist` (line 333), `Run_parse` (line 348), `Scope_generate_casepo` (line 362), `Run_status_evaluate` (line 450); NO `Run_enrich` or `Run_case_resolve` anywhere (file ends line 468). (3) Corroborated in `CURRENT_STATUS.md:111-112` ("REPO-TRAILS-LIVE ... does not contain ... Run_enrich/Run_case_resolve, the documented pre-existing drift") and `CURRENT_STATUS.md:150` ("Repo intake.definition.json still trails live on action wiring (Run_enrich + Run_case_resolve) — documented drift; live is authoritative"), and memory `intake-repo-trails-live`. (4) The mechanism is real: line 4 comment confirms downstream chaining uses Run-a-Child-Flow Workflow actions that ARE part of the exported solution definition, so a CollisionSpikeFlows solution re-import from this stale def would drop the two child-flow cards and regress live auto-merge + enrichment wiring. The recommendation (reconcile the repo def; flag not-safe-to-reimport in DEPLOY-RUNBOOK) is sound and additive.

**Verifier correction:** Not wrong, but a severity nuance: this is a FULLY DOCUMENTED, intentionally-tolerated drift (flagged in the flow comment + 2 CURRENT_STATUS callouts + memory), not a latent/unknown defect, and `CURRENT_STATUS:150` establishes the governing rule "live is authoritative." The "high" rating reflects the worst-case full-solution-re-import scenario; the current live-edit workflow uses the byte-identical-trigger technique on live flows (not solution re-imports), so the regression only triggers if someone performs a full solution re-import. The finding itself acknowledges this. Medium-high would be more proportionate to day-to-day risk, but high is defensible as a guard against the re-import footgun.

## [MEDIUM] EVA extraction-mapping logic is triplicated across parser_adapter, ocr adapter (and a 3rd vendored exporter)

**Location:** `functions/parser/parser_adapter.py:99-317` and `ocr/ocr_pdf_adapter.py:406-440` (plus the `EVA_FIELD_ORDER`/`EVA_KEY_FROM_PARSER_KEY` constants in each)

**Evidence:** `ocr/ocr_pdf_adapter.py:404` carries the comment `# Envelope projection (identical to functions/parser/parser_adapter.py)` and re-defines `_to_eva_extraction`, `_to_field_cell`, `EVA_FIELD_ORDER`, and `EVA_KEY_FROM_PARSER_KEY` byte-for-byte against `parser_adapter.py` (the only diff is the source fallback string `"ocr_extraction"` vs `"pdf_extraction"`). Because parser (FC1) and ocr (ACA container) are separately-packaged Function apps they can't trivially share a module, so a parity test exists — `ocr/tests/test_eva_map_in_sync_with_parser.py` — but see the separate finding: that test is NOT in the verify-all gate, so the duplication can silently drift.

**Recommendation:** Keep the duplication if a shared package is impractical, but ensure the existing ocr parity test actually runs in CI (add ocr to `verify-all.mjs`). Alternatively vendor a single tiny `eva_map.py` into both packages and diff-test it the same way the schema is.

**Verifier note (confidence: high):** VERIFIED and accurate. The EVA extraction-mapping logic is duplicated across two separately-packaged Function apps: `functions/parser/parser_adapter.py` (`EVA_FIELD_ORDER` L99-112, `EVA_KEY_FROM_PARSER_KEY` L119-132, `to_eva_extraction` L259-298, `_to_field_cell` L301-317) and `ocr/ocr_pdf_adapter.py` (`EVA_FIELD_ORDER` L61-74, `EVA_KEY_FROM_PARSER_KEY` L76-89, `_to_eva_extraction` L406-422, `_to_field_cell` L425-439). A literal diff confirms the constants and the projection logic are identical; the ONLY functional difference is the source fallback string (`"pdf_extraction"` L308 vs `"ocr_extraction"` L430). The "Envelope projection (identical to functions/parser/parser_adapter.py)" comment is present at `ocr/ocr_pdf_adapter.py:404`, and the byte-identical intent is stated at `ocr/ocr_pdf_adapter.py:57-60`. The two are genuinely separate Function apps (`ocr/` has its own `host.json`, `requirements.txt`, `conftest.py`), so a shared module is non-trivial — the duplication is a defensible design choice, correctly reflected in the medium severity.

The load-bearing sub-claim also holds: the parity guard `ocr/tests/test_eva_map_in_sync_with_parser.py` exists (3 tests asserting `EVA_FIELD_ORDER`/`EVA_KEY_FROM_PARSER_KEY` match across both adapters) but is NOT wired into the offline gate. `verify-all.mjs` (the sole gate — there are no `.github/workflows`) only iterates `for (const fn of ['parser', 'enrichment'])` at L79 and runs pytest against `functions/parser/tests` and `functions/enrichment/tests` (L84); it never touches the `ocr/` directory. Grep for "ocr" in `verify-all.mjs` and across all `*.mjs`/`*.yml` CI files returns no hits. So the guard that would catch drift never runs in CI, meaning the duplication can silently diverge exactly as the finding states. The recommendation (add ocr to `verify-all.mjs`, or vendor a shared `eva_map.py` + diff-test) is sound. Minor note: the parser also defines `CASE_IDENTITY_PARSER_KEYS` (L135) which the OCR adapter inlines as string literals (L418-419) rather than as a named constant — a trivial extra divergence, not a flaw in the finding.

## [MEDIUM] verify-all.mjs only runs 2 of the live Python Function test suites — the parity/sync guards never execute in the gate

**Location:** `verify-all.mjs:78-88`

**Evidence:** The loop is hard-coded `for (const fn of ['parser', 'enrichment'])` — so pytest runs only for `functions/parser` and `functions/enrichment`. The suites for ocr, evasentry, evavalidation, and box-webhook are never invoked by the aggregate gate (at audit time the now-removed inspection-address matcher's suite was also skipped — that app was torn down 2026-06-23, ADR-0013), including the load-bearing drift guards `ocr/tests/test_eva_map_in_sync_with_parser.py` and `functions/parser/tests/test_schema_vendored_in_sync.py` (the latter IS under parser so it runs; the ocr one does not). CURRENT_STATUS reports '79 passed'/'256 passed' from manual runs, but `node verify-all.mjs` itself does not exercise box-webhook/ocr/evasentry/evavalidation.

**Recommendation:** Extend the array to all six function dirs (or auto-discover any `functions/*/tests` + `ocr/tests`). The infra is already generic — each suite has a `.venv` convention and the run/skip helper handles a missing venv.

**Verifier note (confidence: high):** VERIFIED — every claim in this finding is true against the repo at HEAD.

`verify-all.mjs:79` reads literally `for (const fn of ['parser', 'enrichment']) {` (the finding cites 78-88; the loop body is 79-88). So the aggregate gate runs pytest for ONLY `functions/parser` and `functions/enrichment`.

Inventory (at audit time) confirms the function dirs each with a `tests/` subdir — `functions/{box-webhook, enrichment, evasentry, evavalidation, parser}` (plus the since-removed inspection-address matcher's dir, torn down 2026-06-23, ADR-0013) — plus a SIBLING `ocr/tests` (`ocr/` is at repo root, NOT under `functions/`, confirmed: `functions/ocr/` does not exist). The loop therefore skips box-webhook, evasentry, evavalidation, AND the entire ocr suite.

The two specific drift guards are real and exist where cited:
- `ocr/tests/test_eva_map_in_sync_with_parser.py` EXISTS and is genuinely load-bearing — its docstring states it "fails the moment EVA_FIELD_ORDER or EVA_KEY_FROM_PARSER_KEY diverge across the two adapters" (parser_adapter ↔ ocr_pdf_adapter EVA-map parity). It is NEVER invoked by the gate.
- `functions/parser/tests/test_schema_vendored_in_sync.py` EXISTS and, being under `functions/parser`, IS run — the finding correctly notes this distinction.

The finding's strongest corroboration: `CURRENT_STATUS.md:170` claims `node verify-all.mjs → 7/7` covered "pytest parser 53 + enrichment 29 + ocr 36 + evavalidation 51". I checked git: in BOTH the original commit (2382059) and the commit from that exact era (a8522a3, the last commit to touch the file, 2026-06-20), the loop was already hard-coded `['parser','enrichment']`. So the ocr/evavalidation numbers in the status doc came from MANUAL pytest runs, never from the gate — precisely as the finding asserts ("CURRENT_STATUS reports … from manual runs, but node verify-all.mjs itself does not exercise box-webhook/ocr/evasentry/evavalidation"). The status doc even mislabels these manual results as part of the 7/7 gate, which is itself a small accuracy defect the finding implicitly exposes.

The recommendation is sound and the infra is indeed generic: the loop already probes `.venv/Scripts/python.exe` (win) or `.venv/bin/python` (nix) per dir and the `skip()` helper handles a missing venv (`verify-all.mjs:80-87`), so extending the array to all six function dirs + ocr — or auto-discovering `functions/*/tests` + `ocr/tests` — is a drop-in change.

Not intentional-by-design: nothing in the script header (lines 1-14, which claims to test "every slice ... Azure Functions") or in comments signals that 4 of 6 suites are deliberately excluded; the comment at line 78 says "(parser + enrichment)" descriptively but the header's "every slice" promise is broken. The gap is a genuine coverage hole, not a feature gate. Severity "medium" is fair: the un-run suites include real parity/drift guards whose whole purpose is to fail CI on silent divergence, yet they sit outside the only aggregate gate.

Files: `C:/Users/Alex/Documents/GitHub/collisionspike/verify-all.mjs` (loop at line 79; per-dir venv probe + skip at 80-87); `C:/Users/Alex/Documents/GitHub/collisionspike/ocr/tests/test_eva_map_in_sync_with_parser.py`; `C:/Users/Alex/Documents/GitHub/collisionspike/functions/parser/tests/test_schema_vendored_in_sync.py`; `C:/Users/Alex/Documents/GitHub/collisionspike/CURRENT_STATUS.md:170`.

## [LOW] provider-match flow is orphaned-by-design and its anchored-domain match logic is duplicated verbatim inside intake

**Location:** `flows/definitions/provider-match.definition.json:4` and `flows/definitions/intake.definition.json` (Filter_exact_domain block)

**Evidence:** `provider-match.definition.json:4` states: 'intake currently does the match inline and does NOT call this child, so it is orphaned-by-design until a future wiring routes the match through here' and 'The anchored exact-membership logic (Filter_exact_domain ...) is the CANONICAL match and is mirrored verbatim inside intake.definition.json'. The `Filter_exact_domain` action exists in both files. The live flow inventory (`live-environment.md`) lists 'CS Provider Match ON' but intake never invokes it — it is a parallel, unused copy on the hot path.

**Recommendation:** Accept as intentional (documented ALM/reuse standby) but track it: either route intake's match through the child to kill the verbatim duplication, or add a lint assertion that the two `Filter_exact_domain` expressions stay identical so a fix to one can't miss the other.

**Verifier note (confidence: high):** VERIFIED REAL, but it is an explicitly documented intentional design choice — its only actionable substance is the maintainability/drift risk of the verbatim duplication, which the finding's own recommendation captures. Severity "low" is correct.

Evidence (all re-read directly):
1. `flows/definitions/provider-match.definition.json:4` contains verbatim: "intake currently does the match inline and does NOT call this child, so it is orphaned-by-design until a future wiring routes the match through here" and "The anchored exact-membership logic (Filter_exact_domain ...) is the CANONICAL match and is mirrored verbatim inside intake.definition.json (intake's own inline match is authoritative on the hot path; this flow is kept for ALM/reuse + the image-first re-dedup path)." Quoted accurately.

2. The duplication is byte-identical. The `Filter_exact_domain` "where" expression is the same string in both files:
   - `provider-match.definition.json:73`
   - `intake.definition.json:197`
   both: `@contains(split(replace(replace(toLower(coalesce(item()?['cr1bd_knownemaildomains'], '')), decodeUriComponent('%0D'), ''), ' ', ''), decodeUriComponent('%0A')), variables('senderDomain'))`
   `intake.definition.json:199` even self-documents this: "Identical expression to provider-match.definition.json Filter_exact_domain."

3. Intake does NOT invoke the child. Intake's only child-flow Workflow actions are `CS_Classify_Persist` (`intake.definition.json:336`), `CS_Parse` (:351), `CS_Status_Evaluate` (:453). No `workflowReferenceName` for provider-match exists; intake runs its match inline (`List_active_providers` -> `Filter_exact_domain:193` -> `If_one_provider:202` -> `Create_case_matched`). `intake.definition.json:463` confirms: "case-resolve.definition.json + provider-match.definition.json remain standalone returnable children ... but are NOT in this hot path: intake owns the single Create here."

4. Live inventory: `docs/architecture/live-environment.md:67` lists "CS Provider Match" as state ON, yet it is absent from the documented hot path (lines 117-126). Consistent with the finding.

Caveats that do NOT undermine the finding:
- This is intentional-by-design and the finding correctly says "Accept as intentional." It is a maintainability nit (verbatim-duplication drift risk + an ON-but-unused flow), not a defect.
- Separate known divergence (memory `intake-repo-trails-live`; `intake.definition.json:463` WARNING) is about `Run_enrich`/`Run_case_resolve`, not the provider match, so it does not affect this finding's accuracy.

## [LOW] Dead exports in box-webhook webhook_verify.py (first_present, HDR_SIGNATURE_VERSION)

**Location:** `functions/box-webhook/webhook_verify.py:39` (`HDR_SIGNATURE_VERSION`) and `:258` (`first_present`)

**Evidence:** `HDR_SIGNATURE_VERSION = "box-signature-version"` and `def first_present(headers, names)` are defined but never referenced anywhere in `functions/box-webhook` (`function_app.py` imports header/is_replay/verify_signature/classify_trigger/is_upload/extract_* but not these two), and a grep of `tests/` returns no hits either — they are fully dead, not just test-only.

**Recommendation:** Remove both, or wire `first_present` into the header lookup if a multi-name fallback was intended (the receiver currently reads a single fixed header name per field via `header(...)`).

**Verifier note (confidence: high):** CONFIRMED. Both symbols are genuinely dead. `functions/box-webhook/webhook_verify.py:39` defines `HDR_SIGNATURE_VERSION = "box-signature-version"` and `:258` defines `def first_present(headers, names)`. The consumer `function_app.py` imports exactly 14 webhook_verify symbols (lines 49-64): `DeliveryDedup`, `HDR_DELIVERY_ID`, `HDR_DELIVERY_TIMESTAMP`, `HDR_SIGNATURE_PRIMARY`, `HDR_SIGNATURE_SECONDARY`, `classify_trigger`, `extract_file_id`, `extract_file_name`, `extract_file_sha1`, `extract_folder_id`, `header`, `is_replay`, `is_upload`, `verify_signature` — neither dead symbol is among them. A repo-wide grep for `first_present|HDR_SIGNATURE_VERSION` returns ONLY the two definition lines; nothing references them. Tests are not the consumer either: `test_webhook.py:29-30` imports `webhook_verify as wv` plus `DeliveryDedup`/`is_replay`/`verify_signature`, and grepping the `tests/` dir for the dead names (including via the `wv.` alias) yields zero hits. So the finding's strongest claim ("fully dead, not just test-only") holds. Not intentional-by-design: the sibling `HDR_*` constants are all live-imported, making `HDR_SIGNATURE_VERSION` an inconsistent outlier, and `first_present` is an orphaned helper (its body does use the live `header()`). Severity low is right — box-webhook is the deployed-but-dormant Function (`BOX_API_ENABLED=false`), so this is cosmetic. Minor caveat on the recommendation: Box signature headers have fixed canonical names, so the "wire first_present into a multi-name fallback" alternative has no real use case; plain removal is the correct fix.

## [LOW] Enrichment get_vehicle_summary computes mot_status + latest_mot_mileage that the live path discards

**Location:** `functions/enrichment/analysis.py:507-517`; consumed at `functions/enrichment/function_app.py:171-173`

**Evidence:** `get_vehicle_summary` builds `summary["mot_status"] = mot_status(v)` and `summary["latest_mot_mileage"] = ...mileage_history(v)...`, but `enrich()` only reads `summary.get("model")` and `summary.get("make")`. The docstring concedes this: 'The function_app reads make / model off this; the rest is advisory context the caller may surface but does not require.' So `mot_status()` and `mileage_history()` are invoked on every enrichment purely to populate a dict the caller drops — leftover shape from the retired MCP/structuredContent contract (the file repeatedly references 'the old get_vehicle_summary structuredContent').

**Recommendation:** Low priority: trim `get_vehicle_summary` to compute only make/model for the live path (keep `mot_status`/`mileage_history` if you intend to surface them later), or document them as a forward hook. Harmless but pure compute waste per call.

**Verifier note (confidence: high):** Confirmed by reading the cited code. `functions/enrichment/analysis.py:507-517` — `get_vehicle_summary()` calls `vehicle_summary(v)` then sets `summary["mot_status"] = mot_status(v)` (line 514) and `summary["latest_mot_mileage"] = history[-1]` from `mileage_history(v)` (lines 515-516). The sole live caller, `functions/enrichment/function_app.py:171-173`, reads only `summary.get("model")` and `summary.get("make")` — `mot_status` and `latest_mot_mileage` are never consumed. `mot_status` (`analysis.py:163-193`) does a sort + date arithmetic; `mileage_history` (196+) does a filter + sort, both over the same `_mot_tests(v)` already in memory. The docstring (lines 510-511) and section header (502-505) explicitly state this mirrors the retired MCP structuredContent contract and that the rest is advisory. So the finding is accurate: the two helpers are invoked every enrichment to populate dict keys the caller drops. Severity is correctly self-rated LOW and the finding flags it as harmless: the output is correct, nothing breaks, there is NO extra I/O or API call (the DVSA record is fetched once, `function_app.py:151-153`), so the waste is an in-memory sort/filter over a small list — negligible. Mild overstatement only in 'pure compute waste per call' (magnitude is trivial), which the finding itself concedes with 'Harmless.' It is also defensibly intentional: comments deliberately preserve the structuredContent shape ('the function_app cleaning layer is unchanged') and `README.md:92` documents these as public helpers — the finding's alternative recommendation ('document as a forward hook') is the apt fix rather than trimming.

## [INFO] (Removed) inspection-address matcher dead-helper finding — no longer applicable

The original [INFO] finding here flagged a dead `lookup_outcode` helper in the inspection-address
matcher Function. That entire Function (and its tests/fixtures) was **removed root-and-stem on
2026-06-23** (ADR-0013), so the finding no longer applies. The live inspection-address model is the
offline-derived full-address suggestions corpus + manual confirm — see
`docs/architecture/inspection-address-corpus.md`.

## [INFO] Unused exception bindings in box-webhook _run_box_op

**Location:** `functions/box-webhook/function_app.py:112` and `:132`

**Evidence:** `except BoxConfigError as exc:` (line 112) and the final `except Exception as exc:` (line 132) bind `exc` but never use it — the BoxConfigError branch logs the literal string 'BoxConfigError' and the generic branch logs `type(exc).__name__` only via a sibling pattern. Trivial; flagged for completeness as a linter (F841 / unused variable) nit.

**Recommendation:** Drop the `as exc` on the two branches that don't use it (or switch to `type(exc).__name__` for consistency with the BoxAuthError/BoxError branches).

**Verifier note (confidence: high):** Verified against `functions/box-webhook/function_app.py` lines 105-136. The finding is a real but trivial F841-style cosmetic nit (severity "info" is right — no behavioral impact), and it is partly mis-located. At line 112 `except BoxConfigError as exc:` the binding is genuinely unused — line 113 logs the literal string "BoxConfigError", not exc. However, at line 132 `except Exception as exc:` the binding IS used: line 133 is `logger.warning("box op hard-failed: %s", type(exc).__name__)`, which references exc. The finding's own evidence even concedes "the generic branch logs `type(exc).__name__`", contradicting its title/location claim that 132 is an unused binding. Separately, the finding MISSES the one other genuinely-unused binding: line 122 `except BoxScopeError as exc:` whose handler (line 125) logs the literal "BoxScopeError" and never uses exc — the same defect it flagged for BoxConfigError. By contrast BoxAuthError (115/116) and BoxError (129/130) both use exc.

**Verifier correction:** The genuinely-unused exception bindings are at line 112 (`BoxConfigError`) and line 122 (`BoxScopeError`) — both log a hardcoded class-name string instead of exc. Line 132 (`except Exception as exc`) is NOT an unused binding; line 133 references it via `type(exc).__name__`, so it should be removed from the finding. Corrected recommendation: drop `as exc` from the BoxConfigError (112) and BoxScopeError (122) branches, OR switch their log lines to `type(exc).__name__` to match the BoxAuthError/BoxError/Exception branches.

## Dropped during verification

- **Vendored RJS docx exporter + python-docx are dead weight on the parser Function runtime path** — Refuted: `python-docx` is NOT dead weight; it is a hard, exercised dependency of the `.docx` reader (`readers/docx.py:6,57`), reached via `process_document`→`read_document`→`get_reader_for_path`→`DocxDocumentReader` for any `.docx` attachment. Only the `RJSDocxExporter` class is unused on the Function path, and lazy-importing it would not remove `python-docx` from the FC1 closure, so the recommendation doesn't achieve its goal; `requirements.txt:28` already documents it as the ".docx reader + RJS docx exporter" dependency.
