# Orphaned / built-but-unwired features (#10)

This section catalogues features that are built and (where backed by a Function) deployed live, but are not wired through to a working path — the classic dead-affordance and orphaned-resource failure modes — separated cleanly from artefacts that are unwired *by design*. The genuine orphans cluster in the Code App's manual-intake screen (enrichment + address buttons that always return "not connected" because no connector transport was ever injected) and one latent env-var-gate coherence lag; the EVA submit dialog adds a non-functional duplicate of a working export. All six findings below were independently verified (`verdict.real = true`).

Confirmed findings by severity: **0 critical, 0 high, 2 medium, 3 low, 1 info** (6 total). Note: one of the two MEDIUM findings — the orphaned runtime inspection-address matcher Function + its dead Code App address button — was **resolved 2026-06-23** by removing that whole stack root-and-stem (ADR-0013); see the [RESOLVED 2026-06-23] entry below.

## [MEDIUM] Code App "Look up vehicle (DVLA/DVSA)" button is permanently non-functional — no connector/transport injected, despite the enrichment Function being live

**Location:** `mockup-app/src/screens/ManualIntake.tsx:52-53,525,549`; `mockup-app/src/data/enrichment-client.ts:48-75`; `mockup-app/src/data/generated-services.ts:81-92`; `mockup-app/power.config.json` (connectionReferences)

**Evidence:** ManualIntake imports only `enrichVehicle`/`normaliseAddress` (lines 52-53) and calls `await enrichVehicle(vrm)` (line 525) and `await normaliseAddress(...)` (line 549) with NO transport argument, so both fall back to the module defaults `notConnectedVehicleTransport`/`notConnectedAddressTransport` (enrichment-client.ts:48-55,60,70), which always return `{status:'not_connected'}`. Unlike the parser — which has `parser-connector-transport.ts` (`connectorParserTransport`) injected at ManualIntake.tsx:63,478 — there is NO `enrichment-connector-transport.ts`, and `grep` for `configureEnrich*`/`setVehicleTransport`/`EnrichDvsaMot` across mockup-app/src returns nothing. `generated-services.ts` bridges only the 8 Dataverse tables (no enrichment/address connector), and `power.config.json` registers exactly one non-Dataverse connector (the CE Parser). Meanwhile the backing Function is LIVE: `cespkenrich-fn-gi62sd` Running, gate `ENRICHMENT_ENABLED=true`, connection `cr1bd_dvsaenrich` Bound (live-environment.md:58), connector OpenAPI `functions/enrichment/openapi/enrichment-connector.json` exists with op `EnrichDvsaMot`. The 190626 review (checklist.md:70) acknowledges the button returns 'not connected', and the enrichment-activation plan (§8, lines 95-96) flags the on-demand Code App action as an explicit open question that was never resolved.

**Recommendation:** If the on-demand manual lookup is wanted (the §8 open question), wire it: `pac code add-data-source` for the `cr1bd_dvsaenrich` connector, add an `enrichment-connector-transport.ts` mirroring `parser-connector-transport.ts`, and pass it into `enrichVehicle(vrm, connectorEnrichTransport)` in ManualIntake. If the decision is that enrichment runs only in the flow (it already does, live), then remove or visibly disable the "Look up vehicle" button so the UI does not present a dead affordance. Either way, close enrichment-activation.md §8.

**Verifier (confidence: high):** VERIFIED — finding is accurate. The Code App "Look up vehicle details" button (mockup-app/src/screens/ManualIntake.tsx:815-819, rendered unconditionally, disabled only while enriching/VRM-empty) calls `enrichVehicle(vrm)` at line 525 with NO transport arg; the "Standardise address" button (line 957-960) calls `normaliseAddress(...)` at line 549 likewise. Both fall back to the module-default transports `notConnectedVehicleTransport`/`notConnectedAddressTransport` (enrichment-client.ts:48-55, 60, 70) which hard-return `{status:'not_connected'}`. Unlike the parser — which has parser-connector-transport.ts (`connectorParserTransport`) injected at ManualIntake.tsx:63,478 — there is NO enrichment-connector-transport.ts (Glob of mockup-app/src/data/*transport*.ts returns only parser + box files), and a grep for configureEnrich*/setVehicleTransport/EnrichDvsaMot/enrichment-connector-transport/connectorEnrichTransport across mockup-app/src returns nothing. generated-services.ts (lines 81-92) bridges only the 8 Dataverse tables (no enrichment/address service); generated/services/ contains only the 8 table services + CollisionEngineersParserService (no DVSA service — `pac code add-data-source` was never run for cr1bd_dvsaenrich); power.config.json (lines 12-21) registers exactly one non-Dataverse connector (CE Parser). The backing Function is live: live-environment.md:58 shows cr1bd_dvsaenrich Bound (conn ce0d6944…, → cespkenrich-fn-gi62sd) with ENRICHMENT_ENABLED=true, and CS Enrich flow ON (line 72). The 190626 review (checklist.md:70-71) acknowledges the button returns "not connected"; enrichment-activation.md §8 (lines 96, 315-318) flags the on-demand Code-App action as an open question, noting "The live chain never invokes it today". This is a genuine dead-affordance bug, not an off-by-design gate (a real gate would hide/disable the button; instead it is wired to a default transport that can never succeed). Severity medium is fair. The finding's two-branch recommendation is sound.

**Verifier correction:** Minor under-statement, not a refutation: the §8 "automated-flow vs on-demand-UI" question was de-facto resolved toward the automated flow — enrichment DOES run live in the CS Enrich flow (live-environment.md:72; MEMORY enrichment-activated 2026-06-20). So the on-demand UI button is genuinely redundant for mileage/model, which strengthens the finding's "remove or visibly disable the dead button" branch over the "wire it" branch. Note also the enrichment-activation plan's own facts table (lines 88-89: "Unbound", "gated OFF") trails the live state because it is a pre-activation runbook; the live registry (live-environment.md:58,72) is authoritative and shows the connection Bound and the flow ON.

## [RESOLVED 2026-06-23] Orphaned inspection-address matcher Function + Code App address button — removed root-and-stem (ADR-0013)

The original [MEDIUM] finding here flagged a deployed-but-unreachable runtime inspection-address
matcher Function (no connection reference, no flow caller) plus a dead Code App "Standardise
address" button. **All of it was removed root-and-stem on 2026-06-23** — the Azure Function, its
companion resolve flow, the custom connector, and the dead UI affordance — because it misread `Loc`
(an EVA-export artifact) as a runtime intake input (ADR-0013).

The live inspection-address model is the **offline-derived, full-address-only suggestions corpus**
(`cr1bd_inspectionaddress`) → staff **manual pick/edit** in the CaseDetail Address tab → "Image
Based Assessment" with a reason when unclear, gated by `address-policy.ts`. There is no runtime
matcher. See `docs/architecture/inspection-address-corpus.md` and
`docs/adr/0013-loc-export-artifact-no-runtime-address-matching.md`.

## [LOW] chaser-send flow gates on env-var cr1bd_CHASER_SEND_ENABLED that is never declared in the env-var manifest — gate can never be turned on

**Location:** `flows/definitions/chaser-send.definition.json:46,54-58,75`; `dataverse/environment-variables.json` (absent); `flows/flow-state.json:141`

**Evidence:** chaser-send.definition.json actively reads the gate from Dataverse: `Get_gate_definition` filters `environmentvariabledefinitions` on `schemaname eq 'cr1bd_CHASER_SEND_ENABLED'` (line 46), `Set_gate_CHASER_SEND_ENABLED` coalesces value→default→'false' (lines 54-58), and `If_send_allowed` requires `gate_CHASER_SEND_ENABLED == true` (line 75). But `cr1bd_CHASER_SEND_ENABLED` is NOT in `dataverse/environment-variables.json` — grep across the manifest finds VALUATION/COPILOT/AZURE_VISION but no CHASER_SEND. With no definition row, `Get_gate_definition` returns empty and the coalesce falls through to the literal 'false' (fail-safe OFF), so outbound email can never be enabled because there is no env-var to set true.

**Recommendation:** Add `cr1bd_CHASER_SEND_ENABLED` (Boolean, defaultValue 'false') to dataverse/environment-variables.json so the declared gate matches what chaser-send reads, and let verify-parity.mjs lock it. This is fail-safe today and chaser-send is Phase-2/M2.F deferred, so it is not urgent — but it is a latent gate-with-no-definition that would silently prevent activation when the operator gets there.

**Verifier (confidence: high):** Factual core confirmed but the finding is OVERSTATED on its central claim. CONFIRMED: chaser-send.definition.json reads cr1bd_CHASER_SEND_ENABLED at line 46 ($filter on environmentvariabledefinitions), coalesces value->default->'false' at lines 54-58, requires gate==true at line 75; it is fail-safe OFF when no definition row exists. flows/flow-state.json:141 declares the gate. And it is indeed ABSENT from dataverse/environment-variables.json (and from verify-parity.mjs's hardcoded expect-map at lines 78-89). OVERSTATED/WRONG: (1) "gate can never be turned on / there is no env-var to set true" is FALSE. The definition IS created by dataverse/.build/22-envvars-m2.ps1:40-41 (Boolean, defaultValue 'false') — the Phase-2/M2 deploy script, invoked per dataverse/.build/23-activation-checklist.ps1:82. The flow reads the gate from Dataverse at runtime, not from the JSON file, so once the M2 script runs the row exists and is fully settable to true. (2) environment-variables.json self-declares (line 6) as "The M1 feature-gate manifest"; CHASER_SEND_ENABLED is a Phase-2/M2 gate by design (docs/plans/m2-umbrella-enrichment-to-scale.md:494 calls it a "net-new" M2 gate), so its absence from an M1 manifest is consistent with the split, not a bug. (3) The recommendation "let verify-parity.mjs lock it" misdescribes the tool: verify-parity.mjs only iterates a hardcoded expect map (lines 90-93) and does NOT cross-check flow-read gates against the manifest, so it would not lock this. (4) Already a KNOWN, documented item — docs/architecture/architecture-audit-2026-06-20.md:272-289 (F9) records this exact gap with staged remediation. Net: a real but minor M1-manifest/M2-build-script coherence lag; fail-safe today; the actual activation path is intact. Severity low is fair; the "can never be enabled" framing is the inaccuracy.

**Verifier correction:** The gate CAN be turned on: cr1bd_CHASER_SEND_ENABLED is defined by dataverse/.build/22-envvars-m2.ps1 (the Phase-2/M2 env-var seeding script, Boolean default 'false'), which the operator runs at M2 activation. The flow reads the gate from Dataverse at runtime, so the missing row in the M1 JSON manifest (environment-variables.json) does not block activation — it is a manifest/build-script coherence lag, not a dead gate. The claim "gate can never be turned on" is incorrect. Also, the env-var is absent by design from an explicitly-M1 manifest (it is an M2 gate), this is already documented as a known item (architecture-audit-2026-06-20.md F9), and verify-parity.mjs as written would not "lock" it (it only checks a hardcoded expect-map, not flow-read parity).

## [LOW] Code App Box gates (getBoxGates) read env-var tables that were never added to the generated-services bundle — always returns all-false regardless of live values

**Location:** `mockup-app/src/data/dataverse-source.ts:223-249`; `mockup-app/src/data/generated-services.ts:81-92`; `mockup-app/src/main.tsx:26-84`

**Evidence:** `fetchBoxGates()` reads `services.environmentVariableDefinitions`/`environmentVariableValues` and explicitly returns `{...BOX_GATES_ALL_FALSE}` when those services are absent (dataverse-source.ts:228, comment: "Until the operator wires both env-var tables (pac add-data-source), there is nothing to read"). The `generatedServices` bundle (generated-services.ts:81-92) contains only the 8 Dataverse table services and never the two env-var tables, so `getBoxGates()` can never see a true gate. main.tsx:26-84 documents this as a post-`add-data-source` operator wiring step that has not been done.

**Recommendation:** Correctly deferred-by-design — all five BOX_* gates are OFF live anyway, so all-false is the right behaviour now. But note this is the single seam that gates the entire Phase-7 Code App Box surface: when Box is activated, the operator must add the two env-var Dataverse tables via `pac code add-data-source` and extend `generatedServices`, or every Box affordance stays hidden even after the gates flip. Track it explicitly in the Phase-7 activation runbook.

**Verifier (confidence: high):** VERIFIED and accurate on every load-bearing claim. (1) dataverse-source.ts:228 — `if (!defsSvc || !valsSvc) return { ...BOX_GATES_ALL_FALSE };` with the cited comment at lines 226-227. (2) generated-services.ts:81-92 — the `generatedServices` bundle contains exactly 8 Dataverse table services (cases, evidence, workProviders, inspectionAddresses, auditEvents, fieldProvenance, notes, chasers) and never the two env-var services; those are declared OPTIONAL (`?:`) at types.ts:499,504, so the build compiles without them and `services.environmentVariableDefinitions/Values` read `undefined` at runtime → always returns all-false. (3) main.tsx:26-84 documents this as a post-`add-data-source` operator step that is not done (step 1, lines 34-35: add the two env-var services to generatedServices so getBoxGates reads real values instead of returning all-false). BOX_GATES_ALL_FALSE (types.ts:152-159) is genuinely all-false.

The finding is correctly characterised as deferred-by-design, NOT a current defect: all BOX_* gates are off live (CLAUDE.md / CURRENT_STATUS confirm BOX_API_ENABLED=false, Box dormant), so all-false matches live reality today and nothing is hidden that should be visible. The behaviour is intentional, explicitly commented as the honest-off baseline, and test-covered (box-source.test.ts:54-103 asserts all-false when tables are unwired and real gate values when they are wired). Severity 'low' and the recommendation (track the env-var-table + generatedServices wiring in the Phase-7 activation runbook) are both appropriate. The one real risk it correctly flags: this single seam gates the ENTIRE Phase-7 Code App Box surface — if the operator flips the live gates but forgets to add the two env-var tables via `pac code add-data-source` and extend generatedServices, every Box affordance stays hidden because getBoxGates() keeps returning all-false. Not a bug to fix now; a real activation-time footgun worth runbook tracking. No correction needed.

## [LOW] EvaSubmitDialog "Export for EVA" and "Submit to EVA" are mock toasts — the real EVA JSON export lives only in CaseDetail

**Location:** `mockup-app/src/screens/EvaSubmitDialog.tsx:57,289-299,301-313`; `mockup-app/src/screens/CaseDetail.tsx:870-874,1053`; `mockup-app/src/components/JsonView.tsx:55-80`

**Evidence:** EvaSubmitDialog is self-labelled "MOCK ONLY — Submit and Export fire toasts, never a real network call" (line 57); `onExportJson` (289-299) and `onSubmit` (301-313) only `dispatchToast` and produce no file/no call. The functioning EVA JSON export (download-as-.json) is implemented elsewhere: CaseDetail builds `evaJson` (870-874) and renders it through `JsonView`, whose Download button writes a real Blob (JsonView.tsx:55-80). So the dialog's Export button is a non-functional duplicate of a working feature.

**Recommendation:** Either reuse the real `JsonView`/eva-export download in EvaSubmitDialog so its "Export for EVA" button actually produces the drag-into-EVA file (the dialog is the natural place for it, given it composes the Case/PO), or remove the dialog's Export/Submit buttons to avoid a dead affordance. Direct "Submit" is correctly disabled (EVA_API_ENABLED off / Sentry REST deferred), so only the Export gap matters for M1.

**Verifier (confidence: high):** VERIFIED — finding is accurate. EvaSubmitDialog.tsx self-labels "MOCK ONLY — Submit and Export fire toasts, never a real network call" (line 57). onExportJson (289-299) only dispatchToast("Export ready to drag into EVA") — no Blob, no file, no download. onSubmit (301-313) only dispatchToast + close(), commented "MOCK: no real Sentry call is ever made in M1." The real, working EVA JSON export lives in CaseDetail.tsx: evaJson built at 870-874 from EVA_FIELD_ORDER + liveCase.evaFields, rendered via JsonView at 1053-1057 (EVA-fields tab "EVA submission preview", filename `EVA-${c.casePo||c.id}.json`). JsonView.onDownload (55-80) does a genuine Blob + createObjectURL + anchor-click .json download. The dialog is a route overlay over CaseDetail (line-48 comment), so its toast-only "Export for EVA" button is a dead-affordance duplicate of a feature that actually works one surface over. Severity "low" is correct: this is the mockup-app UI prototype (all 4 dialog/screen files carry mock labelling), the dialog never claimed a real submit, and "Submit directly" is correctly disabled (Radio api disabled line 417; Submit gated by !ready/!complete). Only the Export button is a genuine gap, which the finding scopes correctly ("only the Export gap matters for M1"). Note: task #16 "EVA JSON: copy → download only" is marked completed, consistent with CaseDetail being the canonical download path and the dialog Export being a leftover. Real issue, not an intentional feature gate, correctly rated low.

## [INFO] Deferred-by-design (NOT orphans) — confirmed correctly gated/not-yet-wired

**Location:** `functions/evavalidation/` (cespkeval-fn-6c6fxd); `functions/evasentry/` (cespkeva-fn-ufa3ci); `flows/definitions/box-*.definition.json`; `ocr/` (cespkocr-fn-dev-glju3v); `flows/definitions/chaser-draft.definition.json`, `jobsheet-import.definition.json`, `finalize-eva-box.definition.json`

**Evidence:** These look unwired but cross-check as intentional: (1) evavalidation Function / `ValidateCase` connector is deliberately unused — status-evaluate.definition.json:4 computes readiness INLINE and notes the connector "had NO backing Function" historically; CURRENT_STATUS says the connector is "unused by design". (2) evasentry/EVA-REST is Phase-3c/M2, EVA_API_ENABLED=false by design. (3) The 3 Box flows + box-webhook Function + cr1bd_box_rest connector are Phase-7/ADR-0012, every BOX_* gate OFF; all are registered in flows/flow-state.json:144-175 and gated. (4) OCR host is deployed gated-off (no *_ENABLED flip), Phase 5a. (5) chaser-draft/jobsheet-import/finalize-eva-box are state=off in flow-state.json, later-phase. All are registered, gated, and tracked in ROADMAP/gated.md — not orphaned.

**Recommendation:** No action — these are correctly deferred. Listed so the genuine orphans above (enrichment/address Code App buttons, the undeclared CHASER_SEND gate) are not lumped in with the intentional feature-gates.

**Verifier (confidence: high):** VERIFIED — every sub-claim of this INFO finding holds against the repo. (1) evavalidation/ValidateCase is unused-by-design: status-evaluate.definition.json:4 says readiness is computed INLINE and "the previous shared_evavalidation/ValidateCase connector had NO backing Function (no functions/validation/)"; I confirmed functions/validation/ does not exist, and CURRENT_STATUS.md:321 states "the connector is unused by design". (2) evasentry/EVA-REST is Phase 3c/M2 with EVA_API_ENABLED=false: dataverse/environment-variables.json has cr1bd_EVA_API_ENABLED default 'false'; CURRENT_STATUS.md:321-322 confirms. (3) The 3 Box flows + box-webhook Function + cr1bd_box_rest connector are registered state=off and BOX_*-gated: flow-state.json lines 143-175 register box-folder-create/box-file-request-copy/box-blob-purge all state=off, each gated by a BOX_* var; all 5 cr1bd_BOX_* gates default 'false'. (4) OCR host is deployed gated-off with no *_ENABLED flip (live-environment.md:29; cespkocr-fn-dev-glju3v Running, OCR_PROVIDER=tesseract, no OCR env-var in Dataverse). (5) chaser-draft (no Send action — draft-only), jobsheet-import, finalize-eva-box (gated cr1bd_EVA_API_ENABLED) are all state=off. The finding also correctly EXCLUDES chaser-send and flags the undeclared cr1bd_CHASER_SEND_ENABLED gate as a genuine orphan — corroborated: that gate is referenced by chaser-send.definition.json + flow-state.json:141 but is ABSENT from dataverse/environment-variables.json (declared only in the M2 deploy script 22-envvars-m2.ps1:40), and architecture-audit-2026-06-20.md:277 independently flags it as 'not in' the env-vars. Nit-level imprecisions only: the cited Box-flow span is 144-175 (box-folder-create object actually opens at line 143), and the (1) quote is paraphrased ('historically' is the finding's word, not the source's) — neither affects correctness. Conclusion: these are correctly-classified deferred/feature-gated artefacts, not orphans; the finding is accurate, evidence-grounded, and internally consistent.

## Dropped during verification

_None — no findings were refuted during verification._
