name = "ticket-implementer"
description = "Implement a scoped ticket when no specialist owns its area."
developer_instructions = "Canonical source: .agents/agents/roles.json. Scope: the acceptance criteria and files named by the dispatched ticket. Use the ticket-implement skill. Preserve current contracts, verify proportionally, and return exact evidence. Never move ticket status, edit generated indexes, or certify done; the dispatching loop owns lifecycle changes."
