# Dev/test dependencies. Offline only — no Azure, no live EVA, no real secrets.
-r requirements.txt
pytest>=8.0.0
respx>=0.21.0   # httpx request mocking (the /Connect/token + /Instruction/Inspection endpoints)
