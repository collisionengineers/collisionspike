# Evidence resolvers

`evidence-resolver.mjs` and `evidence_resolver.py` resolve a SHA-256 digest or catalogued logical source
path to its immutable blob under `tests/fixtures/evidence/sha256/`. Callers must not derive storage paths
themselves.
