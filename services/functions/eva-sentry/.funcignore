# Excluded from the deployment package (zip deploy / func azure publish).
# The FC1 remote Oryx build installs requirements.txt on the Linux worker, so
# local venvs, caches, tests, and infra/openapi tooling must NOT ship.
.venv/
.venv_headless/
__pycache__/
*.pyc
.pytest_cache/
tests/
local.settings.json
local.settings.json.TEMPLATE
requirements-dev.txt
infra/
README.md
.gitignore
