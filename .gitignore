# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
out/
*.local

# Vite / React
.vite/
*.tsbuildinfo

# Power Platform / Power Apps code app
.power/
.powerplatform/
*.pac
.pac/

# Environment & secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
local.settings.json

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Test / coverage
coverage/
.nyc_output/

# Playwright MCP scratch output (console logs + page snapshots from UI screenshots)
.playwright-mcp/

# Python (cedocumentmapper_v2.0 tooling, if colocated)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# raw/ — drop-zone for source material to distill (contains real PII:
# claimant data in the job sheet, garage/solicitor contacts). Examined and
# distilled into docs/, but NOT committed. Keep PII out of git history.
raw/
*.pyc
*.egg-info/
boxconfig.json

# test-cases-and-data/ — operator-supplied real case bundles (PII: claimant
# names, VRMs, images). Local analysis input only — never commit. (Like raw/.)
test-cases-and-data/
